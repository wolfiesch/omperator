enabled: false

clusterHost:
  name: t4-cluster
  runtimeProfiles:
    - default
  allowedOrigins: []

kubernetes:
  # Audience accepted by this cluster's API server for projected API credentials.
  apiAudience: https://kubernetes.default.svc

images:
  controller:
    repository: ghcr.io/wolfiesch/omperator-cluster-operator
    digest: ""
    pullPolicy: IfNotPresent
  server:
    repository: ghcr.io/wolfiesch/omperator-cluster-server
    digest: ""
    pullPolicy: IfNotPresent
  sessionRuntime:
    repository: ghcr.io/wolfiesch/omperator-session-runtime
    digest: ""
    pullPolicy: IfNotPresent
  modelGateway:
    repository: ghcr.io/wolfiesch/omperator-model-gateway
    digest: ""
    pullPolicy: IfNotPresent

imagePullSecrets: []

controller:
  replicas: 2
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

server:
  replicas: 3
  terminationGracePeriodSeconds: 60
  # The gateway accepts identity only from Tailscale Serve or the Tailscale Kubernetes operator.
  identityProvider: tailscale
  # At least one trusted Tailscale proxy address or canonical CIDR is required for client access.
  trustedProxyAddresses: []
  trustedProxyCIDRs: []
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: "2"
      memory: 2Gi

session:
  nodeExclude:
    - k3s-worker-02
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: "4"
      memory: 8Gi
  sharedMemorySize: 1Gi
  temporarySize: 2Gi

  omp:
    # Administrator-owned runtime configuration references. The chart creates neither object.
    configMap: ""
    modelsKey: ""
    settingsKey: ""
    # Every provider must use auth: none. Reusable credentials stay outside the
    # session and controller workloads in an administrator or chart gateway.

modelGateway:
  # Optional provider-neutral credential boundary. When enabled, this separate
  # workload alone mounts the existing provider credential Secret.
  enabled: false
  replicas: 2
  upstreamOrigin: ""
  # Exact provider inference paths. Administrative provider APIs stay blocked.
  allowedPaths: []
  existingSecret: ""
  credentialKey: credential
  # Secret value is the complete header value, such as "Bearer ...".
  credentialHeader: authorization
  timeoutMs: 600000
  maxRequestBodyBytes: 16777216
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: "1"
      memory: 512Mi

storage:
  # Required when enabled. The administrator creates this backend-neutral RWX StorageClass.
  adminRWXStorageClass: ""

woodpecker:
  existingSecret: ""
  tokenKey: token
  # Alternative to existingSecret: a short-lived token accepted by an administrator-owned CI adapter.
  serviceAccountAudience: ""
  tokenExpirationSeconds: 600
  configMap: ""
  baseURLKey: baseURL
  webBaseURLKey: webBaseURL
  repositoriesKey: repositories

ingress:
  enabled: false
  className: ""
  host: ""
  annotations: {}
  tls:
    enabled: true
    # Leave empty for the Tailscale ingress class; its operator provisions TLS.
    secretName: ""

networkPolicy:
  enabled: true
  # Empty destination lists fail closed. Supply narrow destinations for enabled components.
  kubernetesApiCIDRs: []
  modelRouteCIDRs: []
  modelRoutePorts: []
  modelRoute:
    # Optional in-cluster model endpoint identity. Set both selectors or neither.
    namespaceSelector: {}
    podSelector: {}
  # Exact provider destinations available only to the optional model gateway.
  modelGatewayUpstreamCIDRs: []
  modelGatewayUpstreamPorts:
    - 443
  ciProviderCIDRs: []
  # This port is inert until a CI destination CIDR or complete selector pair is supplied.
  ciProviderPorts:
    - 443
  ciProvider:
    # Optional in-cluster CI endpoint identity. Set both selectors or neither.
    namespaceSelector: {}
    podSelector: {}
  dns:
    namespaceSelector:
      matchLabels:
        kubernetes.io/metadata.name: kube-system
    podSelector:
      matchLabels:
        k8s-app: kube-dns
  gatewayIngress:
    # Empty selectors fail closed. Set both to the ingress controller identity.
    namespaceSelector: {}
    podSelector: {}
  observability:
    namespaceSelector: {}
    podSelector: {}

observability:
  serviceMonitor:
    enabled: false
    interval: 30s
    labels: {}
  prometheusRule:
    enabled: false
    labels: {}
